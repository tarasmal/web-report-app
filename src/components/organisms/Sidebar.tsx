import React from 'react';
import {Item} from '../../types'

interface Sidebar {
    content: Item[]
}

const Sidebar = () => {
    return (
        <div>

        </div>
    );
};

export default Sidebar;
